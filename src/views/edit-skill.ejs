<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Skill</title>
</head>
<body>
<h1>Editar Skill: <%= skill.name %></h1>
<form action="/skills/<%= skill.skillTree %>/edit/<%= skill.id %>" method="POST">
    <label for="name">Nombre:</label>
    <input type="text" id="name" name="name" value="<%= skill.name %>" required>
    <br>
    <label for="description">Descripción:</label>
    <textarea id="description" name="description" required><%= skill.description %></textarea>
    <br>
    <button type="submit">Guardar Cambios</button>
</form>
</body>
</html>
